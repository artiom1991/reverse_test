/**
 * Функция для преобразования в строку с ограничением по длине.
 * @param {string} props1 
 * @param {number} props2 
 */
export function asString(props1, props2) {
    console.log("asString function!!");
}